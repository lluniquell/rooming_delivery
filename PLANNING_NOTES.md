# 루밍 웹 기획 노트 (2026-06-25)

## 전체 범위 확정

### 웹앱 모듈 2개
1. **차수 관리 + 바코드 검수** (신규 개발)
2. **딜리버리** (이 레포, 기존 스펙 기반)

### 별도
- **오더봇** — Playwright 자동화 스크립트 (E:\claude\rooming_order_bot)
- **바코드 생성기** — Python tkinter (E:\claude\rooming_barcode_generator), 추후 웹 통합 고려

---

## 차수 관리 + 검수 모듈 기획

### 배경
- 카페24를 WMS처럼 사용 중
- 카페24 주문 상태: 상품준비중 → 배송준비중(송장출력) → 배송대기 → 배송중 → 배송완료
- 이카운트 = 재고/상품 마스터, 카페24 = 주문 마스터
- 카페24 상품코드 = 이카운트 품목코드 (동일)

### 차수 구성
- 01~10 고정 생성 (이름 수동 지정, 예: "1차", "예약배송 6/25")
- 나중에 필요시 추가

### 주문 추가 방식
- 카페24 주문번호 입력 → API 조회
- 한 주문 내 상품별로 개별 번호 붙음 (예: 20260625-0000021-01 ~ 05)
- **상품별로** 소품팀/배송팀 셀렉트 + 차수 선택
- 차수 해제(미배정으로 되돌리기) 가능

### 피킹리스트
- 차수별 상품 목록 출력 기능

### 내부 주문 상태 (카페24와 별개)
| 상태 | 의미 |
|------|------|
| `unassigned` | 차수 배정 전 / 차수 해제됨 |
| `assigned` | 차수 배정됨, 검수 전 |
| `partial` | 검수했으나 상품 부족 → 대기 |
| `done` | 검수완료 → 카페24 배송중 업데이트됨 |

### 전체 흐름
```
① 카페24 주문번호 입력 → API 조회
② 상품별 소품팀/배송팀 + 차수 배정
③ 피킹리스트 출력
④ (오프라인) 피킹 → 포장 → 카페24에서 송장 출력 → 운송장번호 생성
⑤ 검수: 운송장 스캔(블루투스, PC) → 주문 조회
⑥ 상품 바코드 스캔 → 이카운트 API → 상품코드 조회 → 주문 대조 → 수량 카운트
⑦ 완료:
   - 전체 일치 → 카페24 배송중 업데이트
   - 부분 부족 →
     옵션A: 부족 상품 제외하고 나머지만 배송중
     옵션B: 배송준비완료(partial) 상태로 대기
```

### 바코드 스캔 로직 (이카운트 API)
```
상품 바코드 스캔
→ 이카운트 API: 바코드로 품목코드 조회
→ 품목코드 = 카페24 상품코드 (동일 체계)
→ 주문서 상품과 매칭 → 수량 카운트

미등록 바코드 시:
→ 그 화면에서 바로 이카운트 API로 바코드 등록 (품목코드 입력)
```

### 바코드 생성기 (E:\claude\rooming_barcode_generator)
- Python tkinter 앱
- 이카운트 품목 엑셀 업로드 → 바코드 채번 → Supabase barcodes 테이블 저장
- 바코드 형식: `200 + YY(2) + MM(2) + W(1) + SERIAL(5)` = 13자리
- Supabase barcodes 테이블: { product_code, product_name, barcode }
- 추후 웹앱에 통합 고려

### 미결 사항
- [ ] 카페24 API에서 운송장번호로 주문 역조회 가능 여부 확인 (개발 시 테스트 필요)
  - 가능 시: `GET /api/v2/admin/orders?tracking_no=xxx`
  - 불가 시: 차수 배정 시 DB에 저장한 주문에 운송장번호 나중에 매핑
- [ ] 이카운트 API 연동 준비 (API 키 발급 등)
- [ ] 카페24 API 앱 등록 (딜리버리와 공유 가능)

---

## 기술 스택 (차수 관리 + 검수)
딜리버리와 별도 앱으로 유지 (무리하게 합치지 않음)
- React + Vite + TypeScript + Tailwind CSS
- Supabase (회사 전용 계정)
- 카페24 REST API
- 이카운트 Open API
